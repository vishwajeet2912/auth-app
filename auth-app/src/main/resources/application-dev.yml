server:
  port: 9092

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth_app
    username: root
    password: Jeet@3558
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

logging:
  level:
    org.hibernate.SQL: debug
#      org.springframework.security:DEBUG

  #JPA and Hibernate configuration
#  spring:

# this properties are imp for production in company
#    hikari :
#      pool-name: HikariCP
#      maximum-pool-size: 5
#      connection-timeout: 10000
#      idle-timeout: 600000
#      max-lifetime: 1800000
#      validation-timeout: 5000
#      leak-detection-threshold: 20000
#      initialization-fail-timeout: 5000

  security:
     jwt:
      secret:${JWT SECRET : defaultAAPpp223kjfjfjsnsns}
      issuer: ${JWT SECRET:api.substring.com  }
      access-ttl-seconds: ${JWT ACCESS TTL SECONDS:3600}
      refresh-ttl-seconds: ${JWT ACCESS TTL SECONDS:86400}
      refresh-token-cookie-name: ${JWT_REFRESH_COOKIE_NAME:refreshToken}
      cookie-secure: ${JWT_COOKIE_SECURE:true}
      cookie-http-only: ${JWT_COOKIE_HTTP_ONLY:true}
      cookie-same-site: ${JWT_COOKIE_SAME_SITE:lax}


